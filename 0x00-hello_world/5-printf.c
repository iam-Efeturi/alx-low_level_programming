#include <stdio.h>
/**
 * main - This program uses printf to print a line.
 * return: Program suceeds when it returns 0
 * usingPrint is the string for the printf function
 * %s\n tells printsf to expect a string and add a new line after it
*/
int main()
{
	char usingPrint[] = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n", usingPrint);
	return 0;
}
