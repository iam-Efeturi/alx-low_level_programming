#include "main.h"
/**
 * main - Program checks if character is an alphabet
 * Return: returns 1 if an alphabet or 0 if otherwise
 * @c: c is what we're checking if an alphabet
*/

int _isalpha(int c);
{
	return (c >= 'a' && c <= 'z' && c
